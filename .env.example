# Optional: Google Maps JavaScript API key for the Google-branded interactive map on the contact page.
# Leave unset to use OpenStreetMap (Leaflet) with all branch pins — no key required.
VITE_GOOGLE_MAPS_API_KEY=
